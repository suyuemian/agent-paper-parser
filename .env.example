# 阿里云灵积 DashScope API Key（必填）
# 获取步骤：
#   1. 访问 https://dashscope.aliyun.com
#   2. 注册/登录阿里云账号
#   3. 开通"模型服务灵积" → 获取 API Key
#   4. 新用户有免费额度，足够开发测试
#
# 没有 Key 也可以启动（python run.py --demo），但只能使用 OCR 功能，
# Agent 智能问答需要 Key。
DASHSCOPE_API_KEY=your-api-key-here
